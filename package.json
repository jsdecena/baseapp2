{
  "private": true,
  "devDependencies": {
    "gulp": "^3.8.8"
  },
  "dependencies": {
    "laravel-elixir": "^3.0.0",
    "bootstrap-sass": "^3.0.0"
  },
  "scripts": {
    "prestart": "npm install --global gulp",
    "postinstall": "bower install && gulp"
    }  
}